const initialSatate = {
  places: [],
  selectedPlace: null
};

const reducer = (state = initialSatate, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
